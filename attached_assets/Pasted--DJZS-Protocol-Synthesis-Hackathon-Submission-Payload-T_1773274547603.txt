# DJZS Protocol — Synthesis Hackathon Submission Payload

> The submission API endpoints go live March 13 with the hackathon kickoff.
> This file contains the prepared payload. Update placeholders during the sprint,
> then execute the curl on Day 9.

## Notes

The skill.md only showed the `/register` endpoint at the time of registration (March 11).
The project creation/submission endpoints will be published when building starts March 13.

**Action required on March 13:**
1. Re-fetch the skill.md: `curl -s https://synthesis.md/skill.md`
2. Look for `POST /projects` or similar submission endpoint
3. Update the curl command below with the correct endpoint and field names

---

## Prepared Submission Data

Use this as the base payload — adapt field names to match the actual API spec when it drops.

```bash
curl -X POST https://synthesis.devfolio.co/projects \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <REDACTED>" \
  -d '{
    "name": "DJZS Protocol",
    "tagline": "Adversarial logic firewall for autonomous AI agents",
    "description": "DJZS Protocol intercepts agent reasoning before execution, runs an 11-code adversarial failure taxonomy (DJZS Logic Firewall), writes trust scores on-chain via DJZSLogicTrustScore on Base Mainnet, mints immutable Proof-of-Logic certificates to Irys, and gates capital deployment through DJZSEscrowLock. If the agent fails audit, capital stays locked. If it passes, escrow releases. The human can verify every step on-chain. Trust is not the default — proof is.",
    "tracks": [
      "agents-that-trust",
      "agents-that-cooperate",
      "agents-that-pay"
    ],
    "links": {
      "github": "https://github.com/UsernameDAOEth/djzs-AI",
      "demo": "https://djzs.ai",
      "video": "[FILL — YouTube/Loom link after recording]"
    },
    "contractAddresses": {
      "DJZSLogicTrustScore": "[FILL]",
      "DJZSStaking": "[FILL]",
      "DJZSEscrowLock": "[FILL]",
      "DJZSAgentRegistry": "[FILL]"
    },
    "onChainArtifacts": [
      "https://basescan.org/tx/0x99b9fcfc64af207771afde1851fa7e569e5161d5bb166dbd000b44743bd1ce23",
      "[FILL — DJZSLogicTrustScore BaseScan link]",
      "[FILL — DJZSEscrowLock BaseScan link]",
      "[FILL — Sample PASS audit TX]",
      "[FILL — Sample FAIL audit TX]",
      "[FILL — Irys certificate PASS link]",
      "[FILL — Irys certificate FAIL link]"
    ],
    "conversationLog": "[FILL — paste full CONVERSATION_LOG.md content or URL]",
    "submissionMetadata": {
      "agentHarness": "other",
      "agentHarnessOther": "Custom stack: Replit environment paired with Claude 3.7 Sonnet (Project Knowledge & Memory) for architectural scaffolding and strict JSON enforcement, with Claude Opus 4.6 for strategic planning and sprint coordination",
      "model": "claude-sonnet-4-5-20250514",
      "additionalModels": ["claude-opus-4-6"],
      "chain": "base-mainnet",
      "identity": "ERC-8004",
      "ens": "djzsx.eth"
    }
  }'
```

---

## Placeholder Checklist

Fill these during the sprint:

- [ ] `contractAddresses.DJZSLogicTrustScore` — after Day 1 deploy
- [ ] `contractAddresses.DJZSStaking` — after Day 1 deploy
- [ ] `contractAddresses.DJZSEscrowLock` — after Day 1 deploy
- [ ] `contractAddresses.DJZSAgentRegistry` — after Day 1 deploy
- [ ] `links.video` — after Day 9 recording
- [ ] `onChainArtifacts` — fill as txns are created during Days 3-5
- [ ] `conversationLog` — finalize on Day 8
- [ ] Verify API endpoint and field names match actual spec (check March 13)

---

## Track Names

The actual track identifiers may differ from what's shown above.
On March 13, check the API for available tracks:

```bash
curl https://synthesis.devfolio.co/tracks \
  -H "Authorization: Bearer <REDACTED>"
```

Update the `tracks` array with the correct identifiers.