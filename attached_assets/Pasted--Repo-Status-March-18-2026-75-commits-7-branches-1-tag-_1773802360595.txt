## Repo Status — March 18, 2026

**75 commits, 7 branches, 1 tag, 2 contributors** (you + Dependabot)

### What Changed Since the Context Dump

| Item | Before | Now |
|------|--------|-----|
| Commits | 50 | **75** (+25 commits) |
| Branches | 6 | **7** (+1) |
| Latest commit | — | `263a25d` — "Add missing env vars to docker-compose: IRYS, SETTLEMENT, TRUST_SCORE…" (Mar 15) |
| `pull_policy: always` | Missing | ✅ In docker-compose.yml |
| Duplicate workflow | Existed | ✅ Deleted (only `docker-publish.yml` remains) |
| DISCLOSURE.md | Needed commit | ✅ Committed (Mar 12) |
| SUBMISSION_PAYLOAD.json | Needed removal | ✅ Removed (Mar 12) |

### Things That Still Need Cleanup

1. **`cache/` folder** — still in repo root (Mar 11). Should be in `.gitignore` and removed from tracking.
2. **`artifacts/` folder** — same issue, still tracked.
3. **`output.json`** — sitting in repo root since initial commit. Judges will see this. What's in it?
4. **`attached_assets/`** — updated Mar 15, likely contains screenshots. Move to `/docs` or remove.
5. **`pyproject.toml` + `uv.lock`** — Python config files in a TypeScript project. Leftover from early prototyping? Remove if unused.
6. **README still references `SUBMISSION_PAYLOAD.json`** — line in the Hackathon Submission section links to it, but you deleted the file. Dead link.
7. **No `.db3` cleanup** — were the XMTP database files removed? Can't tell from the root listing but check subdirectories.
8. **Last commit was March 15** — 3 days ago. Your `BASE_RPC_URL` fix and env var updates to Phala were done in the dashboard, not in the repo's docker-compose. Make sure the repo's `docker-compose.yml` matches what's actually running in Phala (should include `BASE_RPC_URL=${BASE_RPC_URL}`).

### What Looks Good

- README is **excellent** — comprehensive architecture diagram, API docs, failure taxonomy, verification instructions, on-chain artifacts table. Judges will appreciate this.
- CONTRACTS.md with all 4 live BaseScan links ✅
- DISCLOSURE.md committed ✅
- DEMO_SCRIPT.md and CONVERSATION_LOG.md present ✅
- CI/CD workflow cleaned up ✅

### Priority Actions

1. **Push a commit today** updating `docker-compose.yml` to include `BASE_RPC_URL=${BASE_RPC_URL}` (match what's running in Phala)
2. **Remove** `output.json`, `pyproject.toml`, `uv.lock`, `cache/`, `artifacts/` from tracking
3. **Fix the dead SUBMISSION_PAYLOAD.json link** in README
4. **Move `attached_assets/`** to `/docs` or remove
5. **Run the Aave V3 memo** to get your PASS certificate — you still only have FAILs in the Irys certificates table in the README

That last point is critical — **every single certificate in your README shows FAIL**. You need at least one PASS to demonstrate the system works both ways. Have you tried the Aave V3 memo yet?