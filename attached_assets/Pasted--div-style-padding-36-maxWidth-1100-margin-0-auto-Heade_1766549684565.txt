<div style={{ padding: 36, maxWidth: 1100, margin: "0 auto" }}>
  {/* Header */}
  <div style={{ marginBottom: 28 }}>
    <div style={{ fontSize: 12, letterSpacing: "0.28em", color: "rgba(255,255,255,.6)" }}>
      DJZS SYSTEM
    </div>
    <h1 style={{ fontSize: 34, fontWeight: 900, margin: "6px 0" }}>
      Your Zones
    </h1>
    <p style={{ maxWidth: 620, color: "rgba(255,255,255,.7)", fontSize: 15 }}>
      DJZS organizes everything you do into Zones — private, encrypted spaces that
      accumulate knowledge, decisions, and history over time.
    </p>
  </div>

  {/* System Diagram */}
  <div
    style={{
      display: "flex",
      gap: 12,
      alignItems: "center",
      marginBottom: 30,
      flexWrap: "wrap",
      color: "rgba(255,255,255,.65)",
      fontSize: 14,
    }}
  >
    <span>Wallet / ENS Identity</span>
    <span>→</span>
    <strong style={{ color: "#b58cff" }}>Zones</strong>
    <span>→</span>
    <span>Journals · Signals · Predictions · Events</span>
  </div>

  {/* Zones Grid */}
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
      gap: 14,
      marginBottom: 32,
    }}
  >
    {[
      { name: "User Zone", desc: "General coordination & updates" },
      { name: "Signals", desc: "Trade ideas & execution notes" },
      { name: "Predictions", desc: "Markets, votes, and outcomes" },
      { name: "Events", desc: "Calls, launches, milestones" },
      { name: "Receipts", desc: "Payments, proofs, records" },
    ].map((z) => (
      <div
        key={z.name}
        style={{
          padding: 16,
          borderRadius: 16,
          border: "1px solid rgba(255,255,255,.1)",
          background: "rgba(255,255,255,.03)",
        }}
      >
        <div style={{ fontWeight: 700 }}>{z.name}</div>
        <div style={{ fontSize: 13, color: "rgba(255,255,255,.6)", marginTop: 4 }}>
          {z.desc}
        </div>
      </div>
    ))}
  </div>

  {/* Actions */}
  <div
    style={{
      display: "flex",
      gap: 12,
      flexWrap: "wrap",
      alignItems: "center",
    }}
  >
    <button
      style={{
        padding: "12px 18px",
        borderRadius: 12,
        background: "#8b5cf6",
        border: "none",
        fontWeight: 700,
        color: "#000",
        cursor: "pointer",
      }}
    >
      Enter User Zone
    </button>

    <button
      style={{
        padding: "12px 18px",
        borderRadius: 12,
        background: "rgba(255,255,255,.05)",
        border: "1px solid rgba(255,255,255,.15)",
        fontWeight: 600,
        color: "rgba(255,255,255,.9)",
        cursor: "pointer",
      }}
    >
      Create New Entry
    </button>

    <span style={{ fontSize: 13, color: "rgba(255,255,255,.55)" }}>
      Your Zone Agent will summarize activity automatically
    </span>
  </div>
</div>