/* src/pages/Landing.tsx */
import { useState } from "react";
import ConnectDrawer from "../components/ConnectDrawer";

export default function Landing() {
  const [drawerOpen, setDrawerOpen] = useState(false);

  return (
    <main className="font-sans text-gray-900 bg-white">

      {/* ==================== HERO ==================== */}
      <section className="relative bg-gradient-to-br from-indigo-700 via-purple-700 to-indigo-900 text-white py-28 px-6 text-center">
        <h1 className="text-4xl md:text-5xl font-bold mb-5">
          Private. Structured. Yours.
        </h1>

        <p className="text-lg md:text-xl max-w-2xl mx-auto mb-10 opacity-95">
          DJZS is a zone-based operating system for private journaling, signals,
          predictions, and coordination — encrypted and owned by your wallet.
        </p>

        <div className="flex flex-col sm:flex-row justify-center gap-4">
          <button
            onClick={() => setDrawerOpen(true)}
            className="bg-white text-indigo-700 font-semibold py-3 px-7 rounded-md hover:bg-gray-100 transition"
          >
            Enter Your Zones →
          </button>

          <button
            onClick={() => alert("Demo video placeholder")}
            className="border border-white/80 py-3 px-7 rounded-md hover:bg-white hover:text-indigo-700 transition"
          >
            Watch 60s Overview
          </button>
        </div>

        <p className="mt-6 text-sm opacity-80">
          Your ENS is your identity. Your Zones compound knowledge over time.
        </p>
      </section>

      {/* ==================== SYSTEM OVERVIEW ==================== */}
      <section className="py-20 bg-gray-50">
        <div className="max-w-4xl mx-auto text-center mb-12">
          <p className="text-sm tracking-widest uppercase text-gray-500 mb-3">
            DJZS System
          </p>
          <h2 className="text-3xl font-bold mb-4">Your Zones</h2>
          <p className="text-gray-700">
            Zones are private, encrypted workspaces that accumulate decisions,
            research, and outcomes. Each Zone has structure, memory, and optional
            AI assistance.
          </p>
        </div>

        <p className="text-center text-sm uppercase tracking-wider text-gray-500 mb-10">
          Wallet / ENS Identity → <strong>Zones</strong> → Notes · Signals ·
          Predictions · Events · Receipts
        </p>

        <div className="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto">
          <ZoneCard
            title="User Zone"
            desc="General coordination, notes, and shared context"
          />
          <ZoneCard
            title="Signals"
            desc="Structured trade ideas, execution notes, and outcomes"
          />
          <ZoneCard
            title="Predictions"
            desc="Markets, votes, and resolved decisions"
          />
          <ZoneCard
            title="Events"
            desc="Calls, launches, milestones, and timelines"
          />
          <ZoneCard
            title="Receipts"
            desc="Payments, proofs, and verifiable records"
          />
        </div>

        <div className="mt-10 flex flex-col sm:flex-row justify-center gap-4">
          <button className="bg-indigo-600 text-white py-3 px-6 rounded-md hover:bg-indigo-700">
            Enter User Zone
          </button>
          <button className="bg-gray-200 py-3 px-6 rounded-md hover:bg-gray-300">
            Create New Entry
          </button>
        </div>

        <p className="mt-3 text-sm text-gray-600 text-center">
          Zone Agents can summarize activity and maintain long-term memory.
        </p>
      </section>

      {/* ==================== WHAT YOU DO IN DJZS ==================== */}
      <section className="py-16 bg-white">
        <div className="max-w-5xl mx-auto grid md:grid-cols-3 gap-10 text-center">
          <Feature
            title="Journal & Research Privately"
            desc="Write notes and research inside encrypted Zones owned by your wallet."
          />
          <Feature
            title="Commit Signals & Predictions"
            desc="Create structured signals and prediction markets with clear outcomes."
          />
          <Feature
            title="Coordinate & Record"
            desc="Manage events, receipts, and decisions in one secure system."
          />
        </div>
      </section>

      {/* ==================== PRIVACY & OWNERSHIP ==================== */}
      <section className="py-20 bg-gray-50">
        <div className="text-center mb-12">
          <h2 className="text-3xl font-bold mb-4">Privacy by Design</h2>
          <p className="text-gray-600">
            Encryption, identity, and ownership are built into the system — not
            bolted on.
          </p>
        </div>

        <div className="max-w-4xl mx-auto grid md:grid-cols-3 gap-6">
          <PrivacyCard
            title="End-to-End Encrypted"
            desc="All Zone entries are encrypted with XMTP. Only participants can read them."
          />
          <PrivacyCard
            title="ENS-Based Identity"
            desc="Your ENS name is your username. No accounts or passwords required."
          />
          <PrivacyCard
            title="Wallet-Owned Data"
            desc="Zones are portable, exportable, and verifiable — your data stays yours."
          />
        </div>
      </section>

      {/* ==================== HOW IT WORKS ==================== */}
      <section className="py-20 bg-white">
        <h2 className="text-3xl font-bold text-center mb-12">
          How DJZS Works
        </h2>

        <div className="max-w-4xl mx-auto grid md:grid-cols-2 gap-8">
          <Step
            title="1 · Connect Your Wallet"
            desc="Your wallet and ENS establish your identity across the system."
          />
          <Step
            title="2 · Enter or Create Zones"
            desc="Zones are purpose-built spaces for focused work and coordination."
          />
          <Step
            title="3 · Commit Structured Entries"
            desc="Notes, signals, predictions, events, and receipts become durable Zone entries."
          />
          <Step
            title="4 · Let Agents Assist"
            desc="Zone Agents summarize activity, extract actions, and maintain memory."
          />
        </div>
      </section>

      {/* ==================== FINAL CTA ==================== */}
      <section className="py-24 bg-indigo-700 text-white text-center">
        <h2 className="text-3xl font-bold mb-5">
          Ready to Enter Your Zones?
        </h2>
        <p className="mb-8 max-w-2xl mx-auto opacity-95">
          DJZS is used by traders, builders, researchers, and DAOs who need
          privacy, structure, and long-term clarity.
        </p>
        <button
          onClick={() => setDrawerOpen(true)}
          className="bg-white text-indigo-700 font-semibold py-3 px-8 rounded-md hover:bg-gray-100"
        >
          Enter Your Zones →
        </button>
      </section>

      {drawerOpen && <ConnectDrawer onClose={() => setDrawerOpen(false)} />}
    </main>
  );
}

/* ==================== SMALL COMPONENTS ==================== */

function ZoneCard({ title, desc }: { title: string; desc: string }) {
  return (
    <div className="border border-gray-200 rounded-lg p-6 hover:shadow-lg transition bg-white">
      <h4 className="font-semibold text-xl mb-2">{title}</h4>
      <p className="text-gray-600">{desc}</p>
    </div>
  );
}

function Feature({ title, desc }: { title: string; desc: string }) {
  return (
    <div>
      <h4 className="font-semibold text-xl mb-3">{title}</h4>
      <p className="text-gray-600">{desc}</p>
    </div>
  );
}

function PrivacyCard({ title, desc }: { title: string; desc: string }) {
  return (
    <div className="bg-white p-6 rounded-lg shadow">
      <h4 className="font-semibold text-xl mb-2">{title}</h4>
      <p className="text-gray-600">{desc}</p>
    </div>
  );
}

function Step({ title, desc }: { title: string; desc: string }) {
  return (
    <div className="bg-gray-50 p-6 rounded-lg shadow">
      <h4 className="font-semibold text-xl mb-2">{title}</h4>
      <p>{desc}</p>
    </div>
  );
}